server:
  port: 1001
  servlet:
    context-path: /aircraft-service
    
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/aircraft_db?useSSL=false
    username: root
    password: Ati@1234 
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
   
    hibernate:
      ddl-auto: update
    show-sql: true
      
  application:
    name: aircraft-service